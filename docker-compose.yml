version: '3'
services:
  admin-panel-dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: admin-panel-dev
    ports:
      - "3200:3000"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
